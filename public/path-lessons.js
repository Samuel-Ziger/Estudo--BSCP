window.COURSE_LESSONS['path-traversal'] = [
  ['Path Traversal Basics', `<span class="lesson-step">01 · FUNDAMENTALS</span><h2>Controle de filename pode virar controle de caminho</h2><p>Path Traversal permite ler arquivos fora do diretório pretendido. Dependendo da API e das permissões, também pode permitir escrita e comprometimento do servidor.</p><pre>/loadImage?filename=218.png<br>base: /var/www/images/<br>resultado: /var/www/images/218.png</pre><p>Se a entrada não for validada, <code>../</code> sobe um nível:</p><pre>filename=../../../etc/passwd<br>/var/www/images/../../../etc/passwd<br>→ /etc/passwd</pre><div class="analogy"><b>Mental model:</b> entrada + diretório base → decoding → normalização → caminho final → operação de arquivo.</div>`],
  ['Attack Methodology', `<span class="lesson-step">02 · PROCESS</span><h2>Descubra a operação e o ambiente</h2><ol class="study-list"><li>Identifique parâmetros como <code>filename</code>, <code>path</code>, <code>file</code> ou <code>template</code>.</li><li>Registre um arquivo válido e a resposta-base.</li><li>Descubra o diretório base e o sistema operacional.</li><li>Teste uma representação por vez.</li><li>Compare status, tamanho, MIME type, corpo e tempo.</li><li>Prove impacto somente com arquivos fictícios do lab.</li></ol><div class="compare"><div><small>UNIX</small><pre>../<br>/etc/passwd</pre></div><div><small>WINDOWS</small><pre>../ ou ..\<br>C:\Windows\win.ini</pre></div></div><p>No Burp Repeater, preserve a requisição bruta. Algumas camadas podem decodificar ou normalizar antes da aplicação.</p>`],
  ['Simple & Absolute Paths', `<span class="lesson-step">03 · BASIC BYPASSES</span><h2>Bloquear ../ não bloqueia todos os caminhos</h2><p>Se a aplicação aceita um caminho absoluto, o atacante pode referenciar diretamente o alvo:</p><pre>filename=/etc/passwd</pre><p>Uma defesa que remove <code>../</code> apenas uma vez também pode ser contornada com sequências aninhadas:</p><pre>....// → remove o ../ interno → ../<br>....\/ → pode produzir ..\</pre><h3>Como testar</h3><p>Compare a entrada enviada, a entrada após o filtro e o caminho canonicalizado. A evidência precisa mostrar em qual transformação a sequência perigosa reapareceu.</p>`],
  ['URL & Double Encoding', `<span class="lesson-step">04 · ENCODING</span><h2>Cada camada pode decodificar uma vez</h2><p>Servidor web, framework e aplicação podem executar percent-decoding em momentos diferentes.</p><div class="compare"><div><small>URL ENCODING</small><pre>../<br>→ %2e%2e%2f</pre></div><div><small>DOUBLE ENCODING</small><pre>%2e%2e%2f<br>→ %252e%252e%252f</pre></div></div><p>Uma camada pode bloquear <code>../</code>, enquanto outra decodifica a representação depois da validação. Registre a forma bruta e o valor após cada decoding.</p><details><summary>Burp Intruder</summary><p>A lista “Fuzzing - path traversal” ajuda a comparar formas codificadas. Controle o URL-encoding automático para saber exatamente o que foi enviado.</p></details>`],
  ['Base Path Validation', `<span class="lesson-step">05 · PREFIX BYPASS</span><h2>StartsWith antes da canonicalização é insuficiente</h2><p>A aplicação pode exigir que a entrada comece com a pasta esperada:</p><pre>/var/www/images/../../../etc/passwd</pre><p>A string começa corretamente, mas o caminho resolvido termina fora da base.</p><div class="compare"><div><small>VALIDAÇÃO FRACA</small><pre>input.startsWith(base)<br>→ true</pre></div><div><small>VALIDAÇÃO CORRETA</small><pre>canonicalize(base + input)<br>final.startsWith(base + separator)</pre></div></div><p>Considere também links simbólicos, diferenças de caixa e separadores específicos da plataforma.</p>`],
  ['Extension & Null Byte', `<span class="lesson-step">06 · SUFFIX BYPASS</span><h2>Uma extensão válida pode esconder um caminho inválido</h2><p>Algumas aplicações exigem que a entrada termine em <code>.png</code>. Em stacks antigas ou integrações nativas vulneráveis, um null byte pode encerrar a string antes do sufixo:</p><pre>../../../etc/passwd%00.png</pre><div class="compare"><div><small>VALIDADOR</small><pre>termina em .png<br>→ permitido</pre></div><div><small>API VULNERÁVEL</small><pre>trunca em %00<br>→ ../../../etc/passwd</pre></div></div><p>Essa técnica depende da linguagem, biblioteca e versão. O objetivo é entender a discrepância, não assumir que funciona em qualquer ambiente.</p>`],
  ['Evidence & Impact', `<span class="lesson-step">07 · REPORTING</span><h2>Mostre o caminho final, não apenas o payload</h2><ol class="study-list"><li><b>Source:</b> parâmetro controlado pelo usuário.</li><li><b>Base:</b> diretório que a aplicação pretendia restringir.</li><li><b>Transformations:</b> filtros, decoding e normalização.</li><li><b>Sink:</b> API de leitura ou escrita de arquivo.</li><li><b>Resolved path:</b> arquivo final fora da base.</li><li><b>Impact:</b> código, configuração, credenciais ou dados expostos.</li></ol><p>Use o arquivo fictício indicado pelo laboratório. Não é necessário acessar arquivos reais do sistema para demonstrar a falha.</p>`],
  ['Prevention', `<span class="lesson-step">08 · DEFENSE</span><h2>Evite transformar entrada em caminho</h2><p>A defesa mais segura é mapear identificadores permitidos para arquivos definidos pelo servidor, sem aceitar caminhos fornecidos pelo usuário.</p><h3>Quando isso não for possível</h3><ol class="study-list"><li>Use uma allowlist de nomes ou caracteres permitidos.</li><li>Construa o caminho dentro de uma base fixa.</li><li>Canonicalize usando a API da plataforma.</li><li>Confirme que o resultado permanece dentro da base, incluindo o separador.</li><li>Rejeite caminhos absolutos, segmentos de ponto e bytes nulos.</li><li>Execute com permissões mínimas e separe arquivos sensíveis.</li></ol><pre>candidate = canonicalize(join(BASE, userInput))<br>allow only if candidate starts with canonical(BASE) + separator</pre><details><summary>Ordem correta</summary><p>Decodifique de forma controlada, valide, construa e canonicalize uma única vez; depois verifique o confinamento antes de acessar o arquivo.</p></details>`]
  ,['Transformation Pipeline', `<span class="lesson-step">09 · ENGINE</span><h2>A resposta mostra cada transformação do caminho</h2><p>O filesystem é inteiramente virtual, mas as operações de parsing são reais. Cada resposta expõe um array <code>transformations</code> para você reconstruir a ordem da vulnerabilidade.</p><pre>raw-request<br>  ↓ pre-decode-filter<br>decode-1 → decode-2<br>  ↓ prefix/extension check<br>native-null-truncation<br>  ↓ canonical-path</pre><p>Um lab só é concluído quando o caminho canônico sai da base e corresponde a um arquivo virtual existente. Apenas enviar <code>../</code>, <code>%00</code> ou uma palavra relacionada não é suficiente.</p><details><summary>Arquivos virtuais úteis</summary><p><code>/var/www/secret.txt</code>, <code>/var/log/app.log</code>, <code>/var/www/app/config.json</code>, <code>/etc/passwd</code> e <code>C:\\Windows\\win.ini</code>.</p></details>`]
];
