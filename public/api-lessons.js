window.COURSE_LESSONS['api-testing'] = [
  ['Fundamentos', `<span class="lesson-step">01 · FUNDAMENTOS</span><h2>Uma API é um contrato — e uma implementação</h2><p>O teste começa comparando o que a aplicação promete com o que o servidor realmente aceita. Endpoint, método, formato, parâmetros, autenticação e limites formam a superfície observável.</p><pre>contrato: PATCH /api/v2/profile
documentado: displayName, email
implementação: vincula todos os campos recebidos</pre><div class="api-principle"><b>Hipótese:</b> uma diferença só importa quando produz uma decisão ou um estado observável.</div>`],
  ['Reconhecimento', `<span class="lesson-step">02 · RECONHECIMENTO</span><h2>Construa o inventário antes de alterar operações</h2><ol class="study-list"><li>Navegue pela aplicação e registre chamadas de API.</li><li>Identifique caminhos-base e versões.</li><li>Revise documentação humana e legível por máquina.</li><li>Inspecione referências nos arquivos do cliente.</li><li>Compare endpoints documentados, usados e disponíveis.</li></ol><div class="api-surface"><span>INTERFACE</span><i>→</i><span>DOCUMENTAÇÃO</span><i>→</i><span>CLIENTE</span><i>→</i><span>ENDPOINTS</span></div>`],
  ['Documentação', `<span class="lesson-step">03 · OPENAPI</span><h2>Documentação é evidência, não verdade absoluta</h2><p>Contratos OpenAPI, JSON ou YAML revelam caminhos, modelos e operações. Eles podem estar incompletos ou desatualizados, por isso devem ser comparados com o tráfego e com as referências do cliente.</p><pre>/api
/swagger/index.html
/openapi.json
/swagger.json</pre><div class="api-principle"><b>Neste módulo:</b> todos os caminhos pertencem ao portal local fictício; não existe descoberta externa.</div>`],
  ['Métodos HTTP', `<span class="lesson-step">04 · MÉTODOS</span><h2>O mesmo recurso pode esconder operações adicionais</h2><div class="api-method-map"><b>GET</b><span>recuperar</span><b>POST</b><span>criar ou executar</span><b>PATCH</b><span>alterar parcialmente</span><b>DELETE</b><span>remover</span><b>OPTIONS</b><span>observar métodos</span></div><p>Teste os verbos de forma controlada e sempre em objetos fictícios de baixa importância. A correção é uma allowlist por endpoint, acompanhada de autorização para cada operação.</p>`],
  ['Tipos de conteúdo', `<span class="lesson-step">05 · MEDIA TYPES</span><h2>Parsers diferentes precisam aplicar a mesma política</h2><p>Uma operação pode tratar JSON, formulário e XML por caminhos diferentes. Aceitar um formato alternativo sem a mesma validação cria uma inconsistência de segurança.</p><pre>Content-Type: application/json
Content-Type: application/x-www-form-urlencoded

mesma operação → mesma validação → mesma autorização</pre><div class="api-principle"><b>Controle:</b> rejeite tipos inesperados e valide a estrutura depois do parsing.</div>`],
  ['Parâmetros ocultos', `<span class="lesson-step">06 · HIDDEN PARAMETERS</span><h2>Uma entrada não documentada ainda faz parte da superfície</h2><p>Propriedades observadas em respostas, nomes usados pelo domínio e diferenças entre versões ajudam a formular candidatos. Adicione um parâmetro por vez e exija uma mudança reproduzível na resposta.</p><div class="api-compare"><article><small>BASE</small><code>{"query":"training"}</code></article><article><small>TESTE</small><code>{"query":"training","includeDrafts":true}</code></article></div>`],
  ['Atribuição em massa', `<span class="lesson-step">07 · MASS ASSIGNMENT</span><h2>O framework não deve decidir quais campos o usuário controla</h2><p>Vinculação automática transforma propriedades internas em parâmetros editáveis. Examine o objeto devolvido, teste valores inválidos e válidos e consulte o recurso depois da alteração.</p><pre>// vulnerável
Object.assign(user, request.body)

// preferível
user.displayName = body.displayName
user.email = body.email</pre><div class="api-principle"><b>Evidência:</b> PATCH modificado → propriedade vinculada → GET confirma novo estado.</div>`],
  ['Poluição em query', `<span class="lesson-step">08 · SERVER-SIDE PARAMETER POLLUTION</span><h2>A entrada pode virar sintaxe na requisição interna</h2><p>Quando o servidor concatena um valor a uma query interna sem encoding, caracteres como <code>#</code>, <code>&amp;</code> e <code>=</code> podem truncar, adicionar ou substituir parâmetros.</p><pre>entrada: peter%26name=administrator
interno: /search?name=peter&amp;name=administrator&amp;publicProfile=true</pre><p>Registre tanto a entrada quanto a requisição interna resultante.</p>`],
  ['Caminhos e JSON', `<span class="lesson-step">09 · REST E DADOS ESTRUTURADOS</span><h2>A mesma classe atravessa caminhos e estruturas</h2><p>Segmentos REST podem ser normalizados depois da concatenação. Strings podem romper a estrutura de JSON construído manualmente. Nos dois casos, o dado deixa de ser valor e passa a controlar a estrutura.</p><div class="api-compare"><article><small>REST</small><code>peter/../administrator</code></article><article><small>JSON</small><code>name + propriedade inesperada</code></article></div>`],
  ['Prevenção e revisão', `<span class="lesson-step">10 · PREVENÇÃO</span><h2>Reduza o contrato ao comportamento necessário</h2><ul class="study-list"><li>Mantenha documentação e inventário atualizados.</li><li>Proteja documentação que não deve ser pública.</li><li>Permita somente métodos e tipos de conteúdo necessários.</li><li>Use mensagens de erro genéricas.</li><li>Aplique controles em todas as versões.</li><li>Permita explicitamente propriedades editáveis.</li><li>Codifique valores e valide formato antes de requisições internas.</li></ul><div class="api-principle"><b>Processo:</b> observar → formular hipótese → testar → comparar → demonstrar impacto → corrigir.</div>`]
];
